//* To declare multiple variables per line
// separe each with commas
int x = 5, y = 6, z =10;
printf("%d",x + y + z);

//! All variables must be the same type, cant mix in the same declaration


//* Assign the same value to multiple variables
int x, y, z;
x = y = z = 50;

// they all return 50